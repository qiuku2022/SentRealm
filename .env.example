# 环境变量示例（SentRealm）
#
# 复制为仓库根目录 `.env` 后按需填写。`.env` 勿提交 git。
# Miniconda 管理 Python 环境；uv 管理包依赖——见 docs/dev/setup.md。

# LLM API 密钥（可选）。未配置时跳过流水线 LLM 断句步骤。
# 启用时尚需在 Settings 中设置 llm_enabled=true、llm_endpoint、llm_model。
SENTREALM_LLM_API_KEY=
