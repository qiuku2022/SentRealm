# 产品路线图

> **权威范围**仍以 [产品定义与 MVP](./01-product-definition-and-mvp.md) 为准。本文档回答：**做到哪一步了、下一步做什么、依赖与风险是什么**。  
> 技术待决项见 [架构文档 · 待定与后续 ADR](../architecture/README.md)（打开后定位同名一节）。

**文档日期**：2026-09-18

**当前对外状态**：M1–M3 已完成；桌面壳已切 **`1.0.0`**。下一优先：干净机冒烟、代码签名与公开发布。

---

## 1. 里程碑总览

```text
M1 Phase 0 脚手架 ──► M2 Phase 1 MVP 核心 ──► M3 可分发 MVP ──► 正式 1.0.0 ──► M4 增强与二期
     ✅ 已完成              ✅ 已完成              ✅ 已完成           🔜 下一目标        📋 规划中
```

| 里程碑 | 版本意向 | 目标一句话 | 状态 |
|--------|----------|------------|------|
| **M1** Phase 0 | `0.1.0` | 多入口骨架 + HTTP/CLI/MCP 契约可联调 | ✅ 已完成 |
| **M2** Phase 1 | `0.2.0` | 完整预处理流水线 + GUI 核心体验 | ✅ 已完成 |
| **M3** 可分发 MVP | `0.5.4` | 非开发者可安装使用；主流程可独立验收 | ✅ 已完成（作者工作环境多次验证） |
| **正式发版** | `1.0.0` | 干净机冒烟 + 代码签名 + 公开发布 | 🔄 版本已切 `1.0.0`；签名 / 干净机仍待 |
| **M4** 增强 | `1.x` | 工作区体验打磨、词表扩充、可选能力 | 📋 规划中 |
| **二期** | `2.x` 起 | 产品定义中的「后续可扩展」项 | 📋 未立项细节 |

---

## 2. 已完成（M1 / M2 / M3）

### M1 — Phase 0 脚手架

| 交付 | 说明 |
|------|------|
| 四模块骨架 | `core` / `cli` / `gui` / `mcp`（[ADR-007](../architecture/adr/007-multi-entry-modules.md)） |
| Tauri ↔ FastAPI | 本机 `127.0.0.1:17300`，health 就绪（[ADR-006](../architecture/adr/006-tauri-spawn-fastapi.md)） |
| Settings + SQLite | 三入口共用配置（[ADR-004](../architecture/adr/004-sqlite-user-settings.md)） |
| 契约 | OpenAPI、`cli-mcp.md`；preprocess 可为 stub |

### M2 — Phase 1 MVP 核心

| 交付 | 说明 |
|------|------|
| 8 步流水线 | 标点分级与去除 → 空格 → 自然句检测 → 全局规则断句/短行修复 → LLM 发送池（可选）→ 标记 |
| 字词表规则断句 | 禁止硬切；数据在 `break_lexicon/` |
| LLM 发送池 | 批 ≤10；质检（内容守恒 + 有效切分 + 有进展，不做 min/max 硬门禁）；每行最多 3 次返工（[产品定义](./01-product-definition-and-mvp.md#llm-发送池与质检返工)、[ADR-005](../architecture/adr/005-llm-integration-privacy.md)） |
| GUI 核心 | 输入 / 参数 / 对照预览 / 超长高亮 / 复制 |
| 多入口一致 | 相同 Settings + 文稿 → gui/cli/mcp 结果一致 |
| 隐私边界 | LLM 仅池内超长行；密钥环境变量（[ADR-005](../architecture/adr/005-llm-integration-privacy.md)） |

**M2 成功标准**（与产品定义对齐，验收时核对）：

1. 定稿口播稿处理后，剪映文稿匹配「大致可用」
2. 常规篇幅秒级完成
3. 横屏 15 / 竖屏 10 开箱可用，自定义字数可预期
4. 写稿方可直接把结果交给剪辑粘贴

### M3 — 可分发 MVP

目标：让**不熟悉本仓库**的写稿/剪辑同学，能安装并完成「粘贴 → 处理 → 复制 → 剪映」主流程。于 **2026-09-18** 以作者工作环境多次安装包验证关闭（不把严格干净机 / 签名 / 公开发布算进 M3）。

| 项 | 说明 |
|----|------|
| **ADR-008 生产打包** | 已接受并落地：PyInstaller sidecar（见 [ADR-008](../architecture/adr/008-production-packaging.md)） |
| **Windows 安装包** | 可本地构建（`scripts/build_installer.ps1`）；内部 NSIS 可用 |
| **冒烟验收（作者侧）** | 工作环境 NSIS 日常使用约 1 个月，并多次复测；主流程 + 剪映粘贴作者侧满意 |
| **用户手册同步** | 内部安装表述已与 NSIS 用法对齐（[docs/user/](../user/README.md)） |

**M3 完成定义（已满足）**：

- [x] 作者工作环境安装后，无需开发命令即可完成主流程（多次验证；2026-09-18 关闭 M3）
- [x] 默认配置下：无 LLM 密钥也能完成去标点 + 规则断句 + 标记
- [x] 用户手册已与内部 NSIS 用法对齐（安装表述、分发状态）

**明确不做**（仍属产品非目标）：对接剪映开放 API、SRT/ASS 导出、ASR、云端同步、改写润色。完整列表见产品定义「不包含」。

---

## 3. 下一优先：正式 `1.0.0`

M3 已交付可安装的内部包。正式 `1.0.0` 补的是**对外发版证据与信任**，不回头改 M3 完成结论。

### 3.1 必须完成（阻塞正式 1.0）

| 项 | 说明 | 依赖 |
|----|------|------|
| **冒烟验收（干净机）** | 干净 Win10/11 上跑 [packaging.md](../dev/packaging.md) 清单 | 安装包 |
| **代码签名 / 公开发布** | 未做（不阻塞内部包） | — |
| **发版前测试** | ✅ `uv run pytest` 默认集 190 passed（2026-09-18） | — |

### 3.2 强烈建议（不阻塞技术发版，但影响体验）

| 项 | 说明 |
|----|------|
| 首次启动引导 | 横/竖屏预设说明、与剪映的衔接一句提示 |
| 后端失败可读提示 | 「后端未就绪 / 端口占用」对应处理（与 [running-locally](../dev/running-locally.md) FAQ 同源，面向用户改写） |
| LLM 配置向导（可选） | 未配置时明确提示「将跳过语义断句，仅规则+标记」 |
| 工作区持久化打磨 | 若已落地 [ADR-010](../architecture/adr/010-workspace-document-persistence.md)：自动保存、活动项修复、删除确认文案 |

### 3.3 正式 `1.0.0` 完成定义（DoD）

- [ ] 干净 Windows 10/11 上安装后，无需开发环境即可完成主流程
- [ ] 关闭应用后无残留 uvicorn / `sentrealm-api`（见 [packaging.md](../dev/packaging.md)）
- [x] `uv run pytest`（默认集）在发版分支通过（2026-09-18：190 passed；每次对外构建前重跑）
- [ ] 代码签名 / SmartScreen 策略已定，并可正式公开发布

---

## 4. M4 与二期方向（规划，未排期）

以下来自产品定义「后续可扩展」与架构占位，**不承诺版本号与日期**。立项时再拆任务。

### 4.1 产品增强（M4 候选）

| 方向 | 价值 | 备注 |
|------|------|------|
| 断点字词表持续扩充 | 减少标记行、提高剪映一次通过率 | 随真实文稿迭代；配套回归测试 |
| 工作区 / 多文稿体验 | 项目侧栏、历史结果、备份心智 | 对齐 ADR-010；cli/mcp 项目管理非必须 |
| 批量处理 | 多文件管道 | 产品定义标为非 MVP |
| 纯离线模式产品化 | 无 LLM 时的能力说明与 UI 状态 | 技术上 M2 已可跳过 LLM |
| SRT 导出 / 校对辅助 | 剪辑下游 | 需单独产品设计 |

### 4.2 工程与平台

| 项 | 触发条件 | 说明 |
|----|----------|------|
| **ADR-009** uv workspace 物理拆包 | 流水线与依赖稳定 | 多包发布、边界更清晰 |
| macOS / Linux | 有明确用户需求 | `default_config_path` / 工作区路径、WebView 前置 |
| MCP settings / CLI `config` | 二期 | 当前仅 gui 写 Settings |
| CI 强化 | 发版前 | 前端 lint/test、打包冒烟 |

### 4.3 远期（二期+）

- 全文 LLM 断句（相对「仅超长行」）
- 更多字幕工具预设（非仅剪映）
- 历史记录、多设备同步（若做，需重开隐私与存储 ADR）

---

## 5. 依赖关系

```mermaid
flowchart LR
  M2[M2_MVP核心]
  ADR8[ADR-008_打包决策]
  Installer[Windows安装包]
  M3[M3_可分发MVP]
  V1[正式_1.0.0]
  M4[M4_增强]

  M2 --> ADR8
  ADR8 --> Installer
  Installer --> M3
  M3 --> V1
  V1 --> M4
```

| 依赖 | 说明 |
|------|------|
| M3 ← ADR-008 | 无打包方案则无法向非开发者交付 |
| 正式 1.0 ← 干净机 / 签名 | 内部可用不等于对外发版证据齐 |
| 用户手册 ← 实际 GUI | UI 文案/布局变更须回写 `docs/user/` |
| 词表质量 ← 真实样例 | 需收集口播稿样例（脱敏）做回归 |
| LLM 体验 ← 用户自备 API | 产品不捆绑密钥；文档须写清「可选」 |

---

## 6. 风险与缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| **Python 运行时打包体积/兼容性** | 安装包大、杀软误报、路径问题 | ADR-008 早做 spike；限定 Win10/11 x64 |
| **字词表覆盖不足** | 标记行偏多，剪映仍需手改 | 黄金样例 + 持续扩充；UI 明确提示人工改标记行 |
| **剪映版本行为差异** | 「最佳实践」漂移 | 文档注明以用户本地剪映为准；不承诺 100% 零微调 |
| **LLM 不稳定 / 费用** | 断句质量波动、返工增调用 | 默认关闭；质检硬门禁；满 3 次标记；每批 ≤10 行 |
| **端口 17300 冲突** | 应用起不来 | 安装包检测与可读错误；长期可评估换端口策略（需改 ADR-006） |
| **文档与代码漂移** | 手册不可信 | 发版 DoD 含文档核对；API 变更同步 openapi |

---

## 7. 近期建议工作序（给执行用）

sidecar / 内部 NSIS / 用户手册内部表述 / **M3 关闭**（2026-09-18，作者工作环境多次验证）。

1. 在**干净** Win10/11 上按 [packaging.md](../dev/packaging.md) 跑冒烟清单（含退出无残留、无新增 `_MEI*`）
2. 代码签名 / SmartScreen 策略，再谈正式公开发布
3. 准备 `1.0.0`：版本号、勾选 §3.3 DoD、发版前 `uv run pytest`
4. 词表与标记行：持续收集真实口播样例做回归（不阻塞 1.0 技术门槛）
5. 发版或重大迭代前按 [implementation-status.md](../dev/implementation-status.md) 做实现对照

---

## 8. 相关文档

| 文档 | 用途 |
|------|------|
| [产品定义与 MVP](./01-product-definition-and-mvp.md) | 范围与成功标准 |
| [用户使用手册](../user/README.md) | 终端用户怎么用 |
| [架构索引](../architecture/README.md) | ADR 与待定项 |
| [开发指南](../dev/README.md) | 开发者如何构建与测试 |
