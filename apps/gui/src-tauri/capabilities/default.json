{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "SentRealm main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:allow-write-text-file",
    {
      "identifier": "fs:scope",
      "allow": [{ "path": "**" }]
    }
  ]
}
